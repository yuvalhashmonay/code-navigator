# Work Log - 2026-05-14

## Code Navigator Initial Build

- Created the first minimal Code Navigator web app.
- Added a server-side OpenAI Responses API proxy so the API key is not exposed in browser code.
- Added the editable system prompt at `prompts/code-navigator-system-prompt.md`.
- Added a focused UI with code input, mode selection, submit action, clear action, and structured output display.
- Added basic request validation and error handling.

## Quality Check

- Pending: run syntax checks and start the local server.
- Pending: verify API execution with `OPENAI_API_KEY` configured.

## 2026-05-21 Update

- Added local `.env` support in `server.js` so the API key can be stored outside source code.
- Added `.gitignore` to keep `.env` private.
- Added `.env.example` to document required local configuration.

## 2026-05-21 Prompt Framework Update

- Added explicit mode profiles for Beginner, Debug, and Architecture modes.
- Changed the request payload so the selected mode sends a dedicated tone, output structure, and instruction contract.
- Updated the system prompt to treat the mode contract as the authoritative structure.
- Added a visible Mode Contract panel in the UI to make the prompt experience design legible.
- Added a local code-size fallback for oversized snippets instead of sending unreliable analysis requests.
